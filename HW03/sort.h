// Assign all libs in header 
#include <stdio.h>

// --------------------
// function signature to use as function prototype
// --------------------

// pointer to int as A

/* size_t is the size in bytes -- each element in the array -- and is used as the return type by the sizeof operator. Requires stdio.h lib */

// n_elements is the number of items in array pointed by A

// --------------------

void sort(int* A, size_t n_elements);
